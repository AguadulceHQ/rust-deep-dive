use std::fs::File;

fn main() {
    // a crash caused voluntarily
    // panic!("We are dying here but still...hello world! 💀");

    // an unwanted crash
    // let scores = vec![10, 100, 1000];
    // scores[42];
}
